---
services:
  go:
    build:
      context: .
      target: godev
    init: true
    command: ["sleep", "infinity"]
    volumes:
      - ".:/src"
      - "/var/run/docker.sock:/var/run/docker.sock"
      - "go_cache:/root/.cache"
      - "go_pkg:/go/pkg"
volumes:
  go_cache:
  go_pkg:
