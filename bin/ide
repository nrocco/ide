#!/bin/bash

# Calculate the directory where `ide` is located
export IDE_PATH=$(dirname $(dirname "${BASH_SOURCE[0]}"))

# Get the subcommand, and default to `help` if none given
COMMAND=${1-help}
shift

# Execute the subcommand
exec "$IDE_PATH/exec/$COMMAND" "$@"
